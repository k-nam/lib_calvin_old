#include "graph_test.h"

int main(int argc, char* argv[])
{
	lib_calvin_graph::graphTest();	
	/*
	using namespace std;
	using namespace lib_calvin;
	cout << "size of matrix is " << sizeof(matrix<int>) << endl;
	cout << "size of graph is " << sizeof(graph<int, int>) << endl;
	cout << "size of u graph is " << sizeof(undirected_graph<int, int>) << endl;
	*/
	return 0;
}
  
